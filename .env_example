# 火山引擎统一配置（TOS和VOD使用相同的AK/SK）
# 如果TOS和VOD使用不同的AK/SK，可以分别设置TOS_ACCESS_KEY/VOD_ACCESS_KEY
ACCESS_KEY=your-access-key-here
SECRET_KEY=your-secret-key-here

# TOS配置（可选，如果不设置则使用上面的统一配置）
TOS_ACCESS_KEY=${ACCESS_KEY}
TOS_SECRET_KEY=${SECRET_KEY}

# VOD配置（可选，如果不设置则使用上面的统一配置）
VOD_ACCESS_KEY=${ACCESS_KEY}
VOD_SECRET_KEY=${SECRET_KEY}

# 工作流ID
VOD_WORKFLOW_ID=your-workflow-id-here

